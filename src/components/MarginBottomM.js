import styled from 'styled-components';
import { Container } from 'semantic-ui-react';

import spacing from '../atoms/spacing';

const MarginBottomM = styled(Container)`
  margin-bottom: ${spacing.m};
`;

export default MarginBottomM;