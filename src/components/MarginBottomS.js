import styled from 'styled-components';
import { Container } from 'semantic-ui-react';

import spacing from '../atoms/spacing';

const MarginBottomS = styled(Container)`
  margin-bottom: ${spacing.s};
`;

export default MarginBottomS;